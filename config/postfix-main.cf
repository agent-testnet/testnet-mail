# Drop $myhostname from mydestination so mail for gmail.com routes to virtual
# mailboxes instead of attempting local (unix-user) delivery and bouncing.
mydestination = localhost.localdomain, localhost
